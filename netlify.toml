# Netlify build configuration for Roomease
[build]
  command = "bun run build"
  publish = "dist"

# SPA fallback: serve index.html for all routes so React Router works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
